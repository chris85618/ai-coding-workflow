# OOAD Design — LangGraph Agentic Workflow System

> **Generated by**: Stage 5 OOAD (Clean Architecture)
> **Traceable to**: FEA-001..011, CLS-001..018, ALG-001..008
> **Last Updated**: 2026-05-14T19:05+08:00 (v3: AI tool features absorbed)

---

## 1. Bounded Contexts (Updated for Clean Architecture)

| Context | Layer | Modules | UC Coverage |
|---------|-------|---------|-------------|
| Pipeline Orchestration | Domain | pipeline, stage, iteration_loop, workflow_resumer | UC-001, UC-003, UC-010 |
| Traceability Engine | Domain | traceable_id, trace_link, micro_validator, crosslink_tracer | UC-004, UC-005, UC-011 |
| Quality Assurance | Domain | quality_gate, security_auditor, completion_checker | UC-006, UC-007, UC-009 |
| Debt Management | Domain | debt_item | UC-008 |
| Stakeholder Context | Domain | scope_analyzer | UC-002 |
| Workflow Adapters | Adapters | langgraph nodes, mcp adapters, file repos | All UC (infrastructure) |

---

## 2. Class Diagram (Mermaid)

```mermaid
classDiagram
    direction TB

    class Pipeline {
        <<Aggregate Root>>
        -phases: list~Phase~
        -stages: list~Stage~
        -current_position: PhaseOrStage
        -status: PipelineStatus
        +start() void
        +advance() void
        +get_position() PhaseOrStage
    }

    class Stage {
        <<Value Object>>
        -number: int
        -dimensions: list~Dimension~
        -iteration_count: int
        -status: StageStatus
        +run_iteration(artifacts) IterationResult
        +check_fixed_point(findings) FixedPointResult
    }

    class IterationLoop {
        <<Domain Service>>
        +execute(stage, artifacts) StageResult
        -agent_alpha(dims, arts) list~Finding~
        -agent_beta(findings) list~Improvement~
        -step_m(improvements) ValidationResult
        -auto_gate() GateDecision
    }

    class TraceableID {
        <<Aggregate Root>>
        -prefix: IDPrefix
        -sequence: int
        -description: str
        -upstream_links: list~TraceLink~
        -downstream_links: list~TraceLink~
        +add_upstream(target, type) void
        +add_downstream(target, type) void
        +is_orphan() bool
    }

    class TraceLink {
        <<Value Object>>
        -source: TraceableID
        -target: TraceableID
        -link_type: LinkType
        -semantic_valid: bool
    }

    class MicroValidator {
        <<Domain Service>>
        +validate(change) ValidationResult
        -check_structure(id) StepResult
        -check_forward(id) StepResult
        -check_backward(id) StepResult
        -check_semantic(id) StepResult
        -check_orphan(id) StepResult
        -trigger_impact(change) ImpactRecord
    }

    class ImpactAnalyzer {
        <<Domain Service>>
        +analyze(change) ImpactRecord
        -expand_forward(id, visited) list
        -check_upstream_semantic(id) list
        -calculate_blast_radius(down, up) int
        -classify_severity(blast, cross) Severity
    }

    class QualityGate {
        <<Value Object>>
        -thresholds: list~Threshold~
        -audit_layers: list~AuditLayer~
        +evaluate(scan) GateResult
    }

    class DebtItem {
        <<Entity>>
        -id: str
        -source: DebtSource
        -rice_score: float
        -quadrant: Quadrant
        -priority: Priority
        +calculate_rice() float
        +classify_quadrant() Quadrant
    }

    class SecurityAuditor {
        <<Domain Service>>
        +audit_all() AuditReport
        -layer1_cso() AuditResult
        -layer2_agent_shield() AuditResult
        -layer3_skill_fortify() AuditResult
        +is_all_pass() bool
    }

    class CompletionChecker {
        <<Domain Service>>
        +check_readiness() CompletionResult
        -verify_traceability() bool
        -verify_quality_gate() bool
        -verify_security_audit() bool
        -verify_tech_debt() bool
    }

    class WorkflowResumer {
        <<Domain Service>>
        +load_checkpoint(checkpoint) WorkflowSnapshot
        +assemble_summary(snap) RecoverySummary
        +auto_resume(snapshot) void
    }

    class CrossLinkTracer {
        <<Domain Service>>
        +trace_all_directions(changed_id) CrossLinkReport
        -collect_horizontal_links(id) list~TraceLink~
        -verify_adr_validity(adrs) list~AdrResult~
        -verify_nfr_satisfaction(nfrs) list~NfrResult~
        +flag_violations(report) list~Violation~
    }

    class ScopeAnalyzer {
        <<Domain Service>>
        +analyze_charter() list~BG~
        +analyze_stakeholders() list~S~
        +define_scope() list~FEA~
        +red_team_challenge(scope) ChallengeResult
    }

    Pipeline "1" *-- "5" Stage : contains
    Pipeline --> WorkflowResumer : recovered by
    Stage --> IterationLoop : iterated by
    IterationLoop --> MicroValidator : validates via
    IterationLoop --> LLMStrategySelector : selects model via
    IterationLoop --> HookRunner : lifecycle hooks
    MicroValidator --> ImpactAnalyzer : triggers
    MicroValidator --> TraceableID : validates
    TraceableID "1" *-- "*" TraceLink : linked by
    ImpactAnalyzer --> CrossLinkTracer : delegates lateral
    SecurityAuditor --> QualityGate : feeds
    CompletionChecker --> QualityGate : checks
    CompletionChecker --> SecurityAuditor : checks
    ScopeAnalyzer --> TraceableID : creates
    LLMStrategySelector --> ModelConfig : creates
    RepoMap --> ContextAllocation : feeds

    class RepoMap {
        <<Value Object>>
        -symbols: list~SymbolDef~
        -token_count: int
        -file_ranks: dict~str,float~
        +prune_to_budget(budget) RepoMap
        +get_context_string() str
    }

    class HookRunner {
        <<Domain Service>>
        -hooks: dict~HookEvent,list~HookDef~~
        +register(event, hook_def) void
        +execute(event, context) HookResult
    }

    class LLMStrategySelector {
        <<Domain Service>>
        -config: StrategyConfig
        +select(task_type) ModelConfig
        +list_providers() list~str~
    }

    class ModelConfig {
        <<Value Object>>
        -provider: str
        -model: str
        -temperature: float
        -max_tokens: int
    }
```

---

## 3. Sequence Diagrams

### SD-001: UC-003 Iteration Stage (Autonomous)

```mermaid
sequenceDiagram
    participant LG as LangGraph Node
    participant UC as RunIteration UC
    participant IL as IterationLoop
    participant Alpha as Agent α (LLM)
    participant Beta as Agent β (LLM)
    participant MV as MicroValidator
    participant IA as ImpactAnalyzer
    participant MW as MarkdownWriter

    LG->>UC: execute(stage, artifacts)
    UC->>IL: execute(stage, artifacts)
    loop Until fixed point or MAX_ITER
        IL->>Alpha: critique(dimensions, artifacts)
        Alpha-->>IL: findings[]
        IL->>Beta: resolve(findings)
        Beta-->>IL: improvements[]
        loop For each improvement
            IL->>MV: validate(change)
            MV-->>IL: ValidationResult
        end
        IL->>IA: analyze(changes)
        IA-->>IL: ImpactRecord
        alt All YAGNI
            Note over IL: REACHED → auto PASS
        else MAX_ITER reached
            Note over IL: log warning → auto PASS
        else Still converging
            Note over IL: NOT_REACHED → continue loop
        end
    end
    IL->>MW: write_stage_artifacts(docs/)
    IL-->>UC: StageResult
    UC-->>LG: state update
```

### SD-002: UC-004 Micro-Validation (6-Step)

```mermaid
sequenceDiagram
    participant MV as MicroValidator
    participant ALG as ALG-002
    participant IA as ImpactAnalyzer
    participant RCA as RootCauseLeftshift

    Note over MV: Step 0: Format check (PGVG)
    MV->>ALG: check_format(change)
    Note over MV: Step 1: Structure check
    MV->>ALG: check_structure(id)
    Note over MV: Step 2: Forward trace
    MV->>ALG: check_forward(id)
    Note over MV: Step 3: Backward trace
    MV->>ALG: check_backward(id)
    Note over MV: Step 4: Semantic check
    MV->>ALG: check_semantic(id)
    Note over MV: Step 5: Orphan check
    MV->>ALG: check_orphan(id)
    Note over MV: Step 5.5: Lateral links (FR-022)
    MV->>ALG: check_lateral(id)
    Note over MV: Step 5.7: LESSON reuse (FR-023)
    MV->>ALG: check_lesson_reuse(change)
    Note over MV: Step 6: Impact analysis
    MV->>IA: analyze(change)
    Note over MV: Step 7: Change record + RCA
    MV->>RCA: root_cause_leftshift(change)
```

### SD-003: UC-010 Checkpoint Resume (Autonomous)

```mermaid
sequenceDiagram
    participant LG as LangGraph Node
    participant UC as ResumeWorkflow UC
    participant WR as WorkflowResumer
    participant CP as CheckpointRepository
    participant MR as MarkdownReader

    LG->>UC: execute()
    UC->>CP: load_checkpoint()
    alt Checkpoint exists
        CP-->>UC: checkpoint_data
        UC->>WR: load_checkpoint(data)
        WR-->>UC: WorkflowSnapshot
        UC->>WR: auto_resume(snapshot)
        Note over WR: Resume from checkpoint position
    else No checkpoint
        UC->>MR: read_existing_docs(docs/)
        MR-->>UC: existing_artifacts
        Note over UC: Start fresh with existing docs
    end
    UC-->>LG: state update
```

---

## 4. Component Diagram

```mermaid
graph TB
    subgraph "Layer 4: Frameworks"
        MAIN["main.py<br/>Entry + DI"]
        GRAPH["graph.py<br/>StateGraph"]
        CONFIG["config.py"]
    end

    subgraph "Layer 3: Adapters"
        NODES["langgraph/nodes.py"]
        MAPPER["state_mapper.py"]
        GK["gitkraken_adapter.py"]
        ST["sequential_adapter.py"]
        FR["file_repository.py"]
        CP["checkpoint_repository.py"]
        LLM["llm_adapter.py"]
        MW["markdown_writer.py"]
        MR["markdown_reader.py"]

        BUS["in_memory_bus.py"]
    end

    subgraph "Layer 2: Application"
        UC1["UC-001..011"]
        PORTS["ports: repos, gateways,<br/>doc_io, event_bus"]
        DTOS["dtos: workflow_dtos.py"]
    end

    subgraph "Layer 1: Domain"
        MODELS["models: Pipeline, Stage,<br/>TraceableID, TraceLink,<br/>QualityGate, DebtItem"]
        SERVICES["services: IterationLoop,<br/>MicroValidator, ImpactAnalyzer,<br/>SecurityAuditor, etc."]
        ALGS["algorithms: convergence,<br/>micro_validation, blast_radius,<br/>rice_scoring, trace_traversal"]
        EVENTS["events: EVT-001..007"]
        CONTRACTS["contracts: INV-001..019"]
    end

    MAIN --> GRAPH
    MAIN --> CONFIG
    GRAPH --> NODES
    NODES --> MAPPER
    NODES --> UC1
    GK -.implements.-> PORTS
    ST -.implements.-> PORTS
    FR -.implements.-> PORTS
    CP -.implements.-> PORTS
    LLM -.implements.-> PORTS
    MW -.implements.-> PORTS
    MR -.implements.-> PORTS
    BUS -.implements.-> PORTS
    UC1 --> PORTS
    UC1 --> MODELS
    UC1 --> SERVICES
    SERVICES --> MODELS
    SERVICES --> ALGS
    SERVICES --> EVENTS
    MODELS --> CONTRACTS
    SERVICES --> CONTRACTS
```

---

## 5. Port Interfaces Design

### 5.1 Repository Ports

```
TraceableIDRepository (Protocol):
    find_by_id(prefix, sequence) -> TraceableID | None
    find_all_by_prefix(prefix) -> list[TraceableID]
    save(id: TraceableID) -> None
    exists(prefix, sequence) -> bool
    next_sequence(prefix) -> int

CheckpointRepository (Protocol):
    load_checkpoint() -> CheckpointData | None
    save_checkpoint(data: CheckpointData) -> None

DocumentIOGateway (Protocol):
    read_docs(docs_path: str) -> dict[str, str]
    write_doc(docs_path: str, filename: str, content: str) -> None
    list_docs(docs_path: str) -> list[str]
```

### 5.2 Gateway Ports

```
LLMGateway (Protocol):
    critique(dimensions, artifacts) -> list[Finding]     # Agent α
    resolve(findings) -> list[Improvement]               # Agent β

MCPGateway (Protocol):
    git_status(directory) -> GitStatusResult
    git_commit(directory, message, files?) -> CommitResult
    git_branch_list(directory) -> list[str]
    git_log(directory, revision_range?) -> list[Commit]
    think_sequentially(thought, number, total) -> ThinkingResult

SecurityGateway (Protocol):
    run_cso_audit() -> AuditResult                       # Layer 1
    run_agent_shield() -> AuditResult                    # Layer 2
    run_skill_fortify() -> AuditResult                   # Layer 3

QualityGateway (Protocol):
    run_sonarcloud_scan() -> ScanResult
```

### 5.3 Document I/O Port (replaces HITLPresenter)

```
DocumentIOGateway (Protocol):
    read_docs(docs_path: str) -> dict[str, str]         # Read all .md files
    write_doc(docs_path: str, filename: str, content: str) -> None
    list_docs(docs_path: str) -> list[str]
    read_doc(docs_path: str, filename: str) -> str | None
```

### 5.4 Event Bus Port

```
DomainEventBus (Protocol):
    publish(event: DomainEvent) -> None
    subscribe(event_type, handler) -> None
```

---

## 6. LLM vs Deterministic Boundary

| Component | Type | Layer | Rationale |
|-----------|------|-------|-----------|
| ALG-001 convergence | Deterministic | Domain | Severity comparison, loop counter |
| ALG-002 micro_validation | Deterministic | Domain | Rule-based structure/trace checks |
| ALG-003 blast_radius | Deterministic | Domain | Graph traversal + threshold rules |
| ALG-004 rice_scoring | Deterministic | Domain | Pure arithmetic formula |
| ALG-005 trace_traversal | Deterministic | Domain | BFS/DFS graph algorithm |
| Agent α critique | **LLM** | Adapter | Requires contextual understanding |
| Agent β resolve | **LLM** | Adapter | Requires creative synthesis |
| Phase 1 understand | **LLM** | Adapter | Code comprehension |
| Phase 2 charter | **LLM** | Adapter | Business analysis |
| Auto-gate decision | Deterministic | Domain | Fixed-point → PASS; diverging → PASS+warning |
| ALG-006 RepoMapBuilder | Deterministic | Domain | tree-sitter AST + PageRank graph ranking |
| ALG-007 ContextBudget | Deterministic | Domain | Priority-based token allocation |
| ALG-008 ModelSelector | Deterministic | Domain | Strategy Pattern: task_type → model config |

---

## 7. Traceable ID to Source Mapping

| ID | Source File (Clean Architecture path) |
|----|--------------------------------------|
| CLS-001 | `domain/models/pipeline.py` |
| CLS-002 | `domain/models/stage.py` |
| CLS-003 | `domain/services/iteration_loop.py` |
| CLS-004 | `domain/models/traceable_id.py` |
| CLS-005 | `domain/models/trace_link.py` |
| CLS-006 | `domain/services/micro_validator.py` |
| CLS-007 | `domain/services/impact_analyzer.py` |
| CLS-008 | `domain/models/quality_gate.py` |
| CLS-009 | `domain/models/debt_item.py` |
| CLS-010 | `domain/services/security_auditor.py` |
| CLS-011 | `domain/services/completion_checker.py` |
| CLS-012 | `domain/services/scope_analyzer.py` |
| CLS-013 | `domain/services/workflow_resumer.py` |
| CLS-014 | `domain/services/crosslink_tracer.py` |
| CLS-015 | `domain/models/repo_map.py` |
| CLS-016 | `domain/services/hook_runner.py` |
| CLS-017 | `domain/services/llm_strategy_selector.py` |
| CLS-018 | `domain/models/model_config.py` |
| ALG-001 | `domain/algorithms/convergence.py` |
| ALG-002 | `domain/algorithms/micro_validation.py` |
| ALG-003 | `domain/algorithms/blast_radius.py` |
| ALG-004 | `domain/algorithms/rice_scoring.py` |
| ALG-005 | `domain/algorithms/trace_traversal.py` |
| ALG-006 | `domain/algorithms/repo_map_builder.py` |
| ALG-007 | `domain/algorithms/context_budget.py` |
| ALG-008 | `domain/algorithms/model_selector.py` |
| EVT-001..010 | `domain/events/domain_events.py` |
| INV-001..024 | `domain/contracts/invariants.py` |
| UC-001..013 | `application/use_cases/*.py` |
| SC-001..016 | `tests/features/*.feature` |
