[pytest]
pythonpath = src
testpaths = tests
bdd_features_base_dir = tests/features
addopts = --cov=src/agentic_workflow --cov-branch --cov-report=term-missing
filterwarnings =
    error

markers =
    ADR-STR-011: BDD scenario for configuration interpolation
    security: security-related tests
