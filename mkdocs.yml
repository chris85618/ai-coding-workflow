site_name: "Agentic Workflow"  # Will be overridden by docs/macros.py
site_url: ""

plugins:
  - search
  - macros:
      module_name: macros

# Navigation is now managed in pyproject.toml [tool.mkdocs.nav]
# site_description is now managed in pyproject.toml [tool.mkdocs.site_description]

theme:
  name: readthedocs
