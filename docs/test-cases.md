# Test Cases Register — Unified Agentic Workflow System

> **Generated by**: Stage 8 (TDD + 驗證)
> **輸入**: SC-001..SC-011, INV-001..INV-019

---

## 驗證方法說明

本專案為純 Markdown 文件驅動的工作流系統，無運行時程式碼。
「測試」以文件驗證腳本形式執行，驗證 docs/ 結構、ID 完整性、追溯矩陣一致性。

---

## TC-001: 管線啟動驗證

**追溯**: SC-001 (validates)
**類型**: 結構驗證

```
TEST: 子模組存在性檢查
  ASSERT exists("skills/everything-claude-code/")
  ASSERT exists("skills/gstack/")
  ASSERT exists("skills/understand-anything/")
  ASSERT exists("skills/skillfortify/")

TEST: AGENTS.md 進入點完整性
  ASSERT AGENTS.md contains "Phase 0"
  ASSERT AGENTS.md contains "Phase 1"
  ASSERT AGENTS.md contains "Stage 3" through "Stage 8"
  ASSERT AGENTS.md contains "Phase 9"
  ASSERT AGENTS.md contains "Phase 10"
```

---

## TC-002: Phase 2 文件結構驗證

**追溯**: SC-002 (validates)
**類型**: 結構驗證

```
TEST: Phase 2 產出文件存在
  ASSERT exists("docs/project-charter.md")
  ASSERT exists("docs/stakeholder-analysis.md")
  ASSERT exists("docs/scope-definition.md")

TEST: BG ID 完備性
  ASSERT docs/project-charter.md contains "BG-001"
  ASSERT count(BG-xxx) >= 1

TEST: BG→FEA 追溯 [INV-017]
  FOR each BG-xxx:
    ASSERT downstream(BG-xxx, "derives").count >= 1
```

---

## TC-003: 迭代協議驗證

**追溯**: SC-003 (validates)
**類型**: 結構 + 語意驗證

```
TEST: 每個 Stage docs 包含迭代協議
  FOR each stage IN {3, 4, 5, 6, 7, 8}:
    ASSERT stage-{N}.md contains "Step A"
    ASSERT stage-{N}.md contains "Step B"
    ASSERT stage-{N}.md contains "Step M"
    ASSERT stage-{N}.md contains "Step C"
    ASSERT stage-{N}.md contains "HITL"

TEST: Step M 引用 skill files
  FOR each stage IN {3, 4, 5, 6, 7, 8}:
    ASSERT stage-{N}.md contains "micro-validation.md"
    ASSERT stage-{N}.md contains "impact-analysis-exec.md"

TEST: 迭代上限定義 [INV-004]
  ASSERT iter-loop.md contains "MAX_ITERATIONS"
  OR algorithm-specs.md ALG-001 contains "MAX_ITERATIONS = 10"
```

---

## TC-004: ID 系統結構驗證

**追溯**: SC-004 (validates)
**類型**: 自動化驗證

```
TEST: 16 前綴完備性
  prefixes = {BG, S, FEA, FR, NFR, UC, ADR, ALG, CLS, EVT, INV, SC, TC, DEBT, RISK, IMP}
  ASSERT AGENTS.md or governance docs define all 16 prefixes

TEST: ID 格式一致性 [INV-006]
  FOR each ID in traceability-matrix.md:
    ASSERT matches pattern "{PREFIX}-{NNN}"

TEST: 無自連結 [INV-008]
  FOR each TraceLink in traceability-matrix.md:
    ASSERT source != target

TEST: 源頭/末端邊界 [INV-007]
  FOR each BG-xxx: ASSERT no upstream links
  FOR each TC-xxx: ASSERT no downstream links (or is endpoint)
```

---

## TC-005: 影響分析一致性驗證

**追溯**: SC-005 (validates)
**類型**: 語意驗證

```
TEST: 嚴重度分類定義
  ASSERT algorithm-specs.md ALG-003 contains "COSMETIC"
  ASSERT algorithm-specs.md ALG-003 contains "MINOR"
  ASSERT algorithm-specs.md ALG-003 contains "MODERATE"
  ASSERT algorithm-specs.md ALG-003 contains "MAJOR"

TEST: MAJOR 上報定義 [INV-013]
  ASSERT algorithm-specs.md ALG-003 contains "MAJOR"
  ASSERT algorithm-specs.md contains "escalate_to_hitl"

TEST: 影響分析 skill 存在
  ASSERT exists("skills/workflow-skills/impact-analysis-exec.md")
```

---

## TC-006: 安全審計結構驗證

**追溯**: SC-006 (validates)
**類型**: 結構驗證

```
TEST: 三層定義完整 [INV-014]
  ASSERT security-audit-stage5.md contains "Layer 1"
  ASSERT security-audit-stage5.md contains "Layer 2"
  ASSERT security-audit-stage5.md contains "Layer 3"

TEST: 安全審計 skill 存在
  ASSERT exists("skills/workflow-skills/security-audit-3layer.md")

TEST: Stage 5 和 Stage 8 安全審計引用
  ASSERT stage-5-ooad-security.md contains "/cso"
  ASSERT stage-5-ooad-security.md contains "skillfortify"
  ASSERT stage-8-tdd-test-fix.md contains "/cso"
  ASSERT stage-8-tdd-test-fix.md contains "skillfortify"
```

---

## TC-007: SonarCloud 門檻驗證

**追溯**: SC-007 (validates)
**類型**: 結構驗證

```
TEST: 門檻值定義完整
  ASSERT stage-8-tdd-test-fix.md contains "≥ 80%"      # 覆蓋率
  ASSERT stage-8-tdd-test-fix.md contains "≤ 5%"        # 技術債比率
  ASSERT stage-8-tdd-test-fix.md contains "0"            # Critical 漏洞

TEST: SonarCloud skill 存在
  ASSERT exists("skills/workflow-skills/sonarcloud-gate.md")
```

---

## TC-008: 技術債結構驗證

**追溯**: SC-008 (validates)
**類型**: 結構驗證

```
TEST: RICE 公式定義 [INV-015]
  ASSERT algorithm-specs.md ALG-004 contains "reach × impact × confidence"
  ASSERT algorithm-specs.md ALG-004 contains "/ effort"

TEST: 四象限定義
  ASSERT algorithm-specs.md ALG-004 contains "QUICK_WIN"
  ASSERT algorithm-specs.md ALG-004 contains "STRATEGIC"
  ASSERT algorithm-specs.md ALG-004 contains "INCREMENTAL"
  ASSERT algorithm-specs.md ALG-004 contains "AVOID"

TEST: 技術債 skill 存在
  ASSERT exists("skills/workflow-skills/tech-debt-collect.md")
```

---

## TC-009: 追溯矩陣完整性驗證

**追溯**: SC-009 (validates)
**類型**: 自動化驗證

```
TEST: 零孤兒
  FOR each ID in all docs/:
    IF prefix != BG: ASSERT has_upstream(ID)
    IF prefix != TC AND prefix != EVT AND prefix != NFR AND prefix != DEBT:
      ASSERT has_downstream(ID)

TEST: 語意一致性
  FOR each TraceLink(source, target, type):
    ASSERT type is valid for (source.prefix, target.prefix)

TEST: 完成檢查 skill 存在 [INV-016]
  ASSERT exists("skills/workflow-skills/completion-check.md")

TEST: 全鏈雙向可達
  ASSERT forward_reachable(BG-001, TC-xxx) == true
  ASSERT backward_reachable(TC-xxx, BG-001) == true
```

## TC-010: 工作流恢復驗證

**追溯**: SC-010 (validates)
**類型**: 結構 + 語意驗證

```
TEST: workflow-state.md 載入完整性
  ASSERT exists("docs/workflow-state.md")
  ASSERT workflow-state.md contains "Pipeline Position"
  ASSERT workflow-state.md contains "Gate Status"
  ASSERT workflow-state.md contains "Pending Escalations"

TEST: 閘門單調性 [INV-018]
  load snapshot FROM workflow-state.md
  FOR each stage IN snapshot.gate_status:
    IF gate_status[stage] = "✅":
      ASSERT after recovery: gate_status'[stage] = "✅"
      // 已通過的閘門不可降級

TEST: Pipeline Position 不倒退 [INV-001]
  load snapshot FROM workflow-state.md
  ASSERT after recovery: pipeline_position' >= snapshot.pipeline_position

TEST: AGENTS.md Step 0 恢復流程定義
  ASSERT AGENTS.md contains "workflow-state.md"
  ASSERT AGENTS.md contains "恢復工作流"
  ASSERT AGENTS.md contains "workflow-resume.md"

TEST: workflow-resume.md skill 存在
  ASSERT exists("skills/workflow-skills/workflow-resume.md")
```

---

## TC-011: 全鏈影響追溯驗證

**追溯**: SC-011 (validates)
**類型**: 語意驗證

```
TEST: 全方向連結類型完備性
  link_types = {derives, decomposes, implements, realizes, constrains,
                justifies, mitigates, stakeholder-of, models, formalizes,
                emitted-by, covers, verifies, validates, guards}
  ASSERT traceability-matrix.md covers all link_types

TEST: ADR 有效性重驗機制 [INV-019]
  ASSERT impact-analysis-exec.md contains "ADR" OR "cross-link"
  ASSERT micro-validation.md contains "ADR" OR "語意"
  // 確認現有 skill 包含 ADR 重驗邏輯

TEST: NFR 約束保留
  FOR each NFR-xxx in traceability-matrix.md:
    ASSERT has_upstream(NFR-xxx)  // 有 FEA 連結
    // NFR 被違反時應觸發 MAJOR

TEST: CrossLinkTracer 對應 skill 存在
  ASSERT exists("skills/workflow-skills/impact-analysis-exec.md")
  ASSERT exists("skills/workflow-skills/micro-validation.md")
  // CrossLinkTracer 功能由這兩個 skill 協同實現

TEST: SUPERSEDED 機制
  ASSERT traceability-matrix.md contains "取代關係圖" OR "Superseded"
  // 確認 ADR 取代機制已定義
```

---

## 追溯矩陣更新

| TC-xxx | 追溯至 SC | 連結類型 |
|--------|----------|---------|
| TC-001 | SC-001 | validates |
| TC-002 | SC-002 | validates |
| TC-003 | SC-003 | validates |
| TC-004 | SC-004 | validates |
| TC-005 | SC-005 | validates |
| TC-006 | SC-006 | validates |
| TC-007 | SC-007 | validates |
| TC-008 | SC-008 | validates |
| TC-009 | SC-009 | validates |
| TC-010 | SC-010 | validates |
| TC-011 | SC-011 | validates |
