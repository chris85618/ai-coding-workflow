"""Pure deterministic algorithms — no LLM, no I/O.

ALG-001: convergence (iteration fixed-point detection)
ALG-002: micro_validation (6-step validation sequence)
ALG-003: blast_radius (impact graph traversal + severity)
ALG-004: rice_scoring (RICE prioritization formula)
ALG-005: trace_traversal (BFS/DFS graph algorithm)
ALG-006: repo_map_builder (tree-sitter AST + PageRank) — from Aider
ALG-007: context_budget (token budget allocation) — from Aider
"""
