# Formal Verification Spec — LangGraph Agentic Workflow

> **Generated by**: Stage 6 (F1-F6 dimensions)
> **Tool**: `crosshair-tool` (symbolic execution for Python)
> **DbC Library**: `icontract` (PRE/POST/INV decorators)
> **Traceable to**: INV-001..024, CLS-001..018
> **Last Updated**: 2026-05-14T19:10+08:00 (v3: AI tool features INV-020~024)

---

## 1. Verification Strategy

| Method | Coverage | Tool |
|--------|----------|------|
| icontract decorators | Every public method PRE/POST, every class INV | Runtime enforcement |
| crosshair-tool | Every icontract-decorated method | Symbolic execution |
| pytest property tests | INV-001, INV-006, INV-012, INV-015 | Hypothesis-style |
| State machine model check | SM-001..004 | crosshair + manual proof |

**Requirement**: Every method in CLS-001..018 must have at least one icontract decorator.

---

## 2. INV → icontract Mapping

### INV-001: Pipeline Sequence Monotonic

```python
# domain/models/pipeline.py — CLS-001
@icontract.require(lambda self: self.status != PipelineStatus.COMPLETED)
@icontract.require(lambda hitl: hitl == HitlChoice.PASS)
@icontract.ensure(
    lambda OLD, self: POSITION_ORDER.index(self.current_position)
    > POSITION_ORDER.index(OLD.self.current_position)
)
def advance(self, hitl: HitlChoice) -> None: ...
```

### INV-002-v2: Auto-Gate Required for Advance

```python
# Enforced by INV-001's @require: auto_gate must return GateDecision.PASS
# (replaces HITL requirement)
@icontract.require(lambda self: self._last_gate_decision == GateDecision.PASS)
def advance(self) -> None: ...
```

### INV-003: Stage Status Unidirectional

```python
# domain/models/stage.py — CLS-002
@icontract.invariant(
    lambda self: self.status in (StageStatus.PENDING, StageStatus.ITERATING, StageStatus.PASSED)
)
class Stage:
    @icontract.ensure(
        lambda OLD, self: _status_order(self.status) >= _status_order(OLD.self.status)
    )
    def transition(self, new_status: StageStatus) -> None: ...
```

### INV-004: Iteration Count Bound

```python
# domain/models/stage.py — CLS-002
@icontract.invariant(lambda self: self.iteration_count <= 10)
class Stage: ...
```

### INV-005-v2: Step M Before Auto-Gate

```python
# domain/services/iteration_loop.py — CLS-003
@icontract.require(lambda self: self._step_m_completed)
def auto_gate(self) -> GateDecision: ...
```

### INV-006: ID Globally Unique

```python
# domain/models/traceable_id.py — CLS-004
# Enforced at repository level:
@icontract.require(
    lambda self, target, registry: not registry.exists(target.prefix, target.sequence)
)
def register(self, target: TraceableID, registry: TraceableIDRepository) -> None: ...
```

### INV-007: BG No Upstream, TC No Downstream

```python
# domain/models/traceable_id.py — CLS-004
@icontract.require(lambda self: self.prefix != IDPrefix.BG, "BG has no upstream")
def add_upstream(self, target: TraceableID, link_type: LinkType) -> None: ...

@icontract.require(lambda self: self.prefix != IDPrefix.TC, "TC has no downstream")
def add_downstream(self, target: TraceableID, link_type: LinkType) -> None: ...
```

### INV-008: No Self-Link

```python
# domain/models/trace_link.py — CLS-005
@icontract.require(lambda source, target: source != target, "self-link forbidden")
def __init__(self, source: TraceableID, target: TraceableID, link_type: LinkType): ...
```

### INV-009: Semantic Link Type Validity

```python
# domain/models/trace_link.py — CLS-005
VALID_LINK_TYPES: dict[tuple[IDPrefix, IDPrefix], set[LinkType]] = { ... }

@icontract.require(
    lambda source, target, link_type:
        link_type in VALID_LINK_TYPES.get((source.prefix, target.prefix), set())
)
def __init__(self, source, target, link_type): ...
```

### INV-010: 6-Step Sequential Execution

```python
# domain/services/micro_validator.py — CLS-006
@icontract.ensure(
    lambda result: result.steps_executed == [
        "structure", "forward", "backward", "semantic", "orphan", "impact"
    ]
)
def validate(self, change: Change) -> ValidationResult: ...
```

### INV-011: Auto-Fix Max 3

```python
# domain/services/micro_validator.py — CLS-006
@icontract.invariant(lambda self: self._retry_count <= 3)
class MicroValidator: ...
```

### INV-012: Severity Monotonic with Blast Radius

```python
# domain/algorithms/blast_radius.py — ALG-003
@icontract.ensure(
    lambda result, blast_radius:
        (blast_radius == 0 and result == Severity.COSMETIC)
        or (blast_radius > 0)
)
def classify_severity(blast_radius: int, cross_stage: int) -> Severity: ...
```

### INV-013: MAJOR Forces HITL

```python
# domain/services/impact_analyzer.py — CLS-007
@icontract.ensure(
    lambda result: result.severity != Severity.MAJOR or result.hitl_notified
)
def analyze(self, change: Change) -> ImpactRecord: ...
```

### INV-014: All 3 Layers Must Pass

```python
# domain/models/quality_gate.py — CLS-008
@icontract.ensure(
    lambda result, self:
        result != GateResult.PASS
        or all(layer.result == "PASS" for layer in self.audit_layers)
)
def evaluate(self, scan_result: ScanResult) -> GateResult: ...
```

### INV-015: RICE Formula Strict

```python
# domain/algorithms/rice_scoring.py — ALG-004
@icontract.require(lambda effort: effort > 0)
@icontract.require(lambda reach: 1 <= reach <= 100)
@icontract.require(lambda impact: impact in (0.5, 1.0, 2.0, 3.0))
@icontract.require(lambda confidence: 0.5 <= confidence <= 1.0)
@icontract.ensure(
    lambda result, reach, impact, confidence, effort:
        abs(result - (reach * impact * confidence) / effort) < 1e-9
)
def rice_score(reach: int, impact: float, confidence: float, effort: float) -> float: ...
```

### INV-016: All Sub-Checks Must Pass for Ship

```python
# domain/services/completion_checker.py — CLS-011
@icontract.ensure(
    lambda result, self:
        result != CompletionResult.READY
        or (self._trace_ok and self._quality_ok and self._security_ok and self._debt_ok)
)
def check_readiness(self) -> CompletionResult: ...
```

### INV-017: Every BG Has At Least One FEA

```python
# domain/services/scope_analyzer.py — CLS-012
@icontract.ensure(
    lambda result: all(len(bg.downstream_feas) >= 1 for bg in result)
)
def analyze_charter(self) -> list[BG]: ...
```

### INV-018: Gate Status Monotonic on Recovery

```python
# domain/services/workflow_resumer.py — CLS-013
@icontract.ensure(
    lambda OLD, self, snapshot:
        all(
            self._gate_status.get(s, 0) >= snapshot.gate_status.get(s, 0)
            for s in snapshot.gate_status
        )
)
def apply_recovery(self, choice: HitlRecoveryChoice, snapshot: WorkflowSnapshot) -> None: ...
```

### INV-019: ADR Revalidation on Change

```python
# domain/services/crosslink_tracer.py — CLS-014
@icontract.ensure(
    lambda result: all(
        adr.revalidated for adr in result.affected_adrs
    )
)
@icontract.ensure(
    lambda result: all(
        adr.status == "SUPERSEDED" and adr.hitl_notified
        for adr in result.affected_adrs if not adr.valid
    )
)
def trace_all_directions(self, changed_id: TraceableID) -> CrossLinkReport: ...
```

### INV-020: Hook Exit Code Contract

```python
# domain/services/hook_runner.py — CLS-016
@icontract.ensure(
    lambda result, hook_def:
        (result.exit_code == 0 and result.proceed is True)
        or (result.exit_code == 2 and result.proceed is False and hook_def.blocking)
        or (result.exit_code not in (0, 2) and result.proceed is True)
)
def execute(self, event: HookEvent, context: dict) -> HookResult: ...
```

### INV-021: Token Budget Never Exceeded

```python
# domain/algorithms/context_budget.py — ALG-007
@icontract.require(lambda total_budget: total_budget > 0)
@icontract.ensure(lambda result, total_budget: result.total_tokens <= total_budget)
def allocate_budget(total_budget: int, repo_map: RepoMap, current_files: list, task_context: str) -> ContextAllocation: ...
```

### INV-022: Strategy Selects Configured Providers Only

```python
# domain/services/llm_strategy_selector.py — CLS-017
@icontract.ensure(
    lambda result, self: result.provider in self._config.enabled_providers
)
def select(self, task_type: TaskType) -> ModelConfig: ...
```

### INV-023: Atomic Commit Includes All Stage Artifacts

```python
# application/ports/gateways.py — MCPGateway
@icontract.require(lambda files: len(files) > 0)
@icontract.ensure(lambda result: result.committed is True)
def auto_commit(self, directory: str, message: str, files: list[str]) -> CommitResult: ...
```

### INV-024: RepoMap Token Count Within Budget

```python
# domain/algorithms/repo_map_builder.py — ALG-006
@icontract.require(lambda token_budget: token_budget > 0)
@icontract.ensure(lambda result, token_budget: result.token_count <= token_budget)
def repo_map_build(self, project_path: str, token_budget: int) -> RepoMap: ...
```
```

---

## 3. crosshair-tool Verification Commands

```bash
# Verify all contracts in domain layer
crosshair check src/agentic_workflow/domain/models/pipeline.py
crosshair check src/agentic_workflow/domain/models/stage.py
crosshair check src/agentic_workflow/domain/models/traceable_id.py
crosshair check src/agentic_workflow/domain/models/trace_link.py
crosshair check src/agentic_workflow/domain/models/quality_gate.py
crosshair check src/agentic_workflow/domain/models/debt_item.py
crosshair check src/agentic_workflow/domain/services/iteration_loop.py
crosshair check src/agentic_workflow/domain/services/micro_validator.py
crosshair check src/agentic_workflow/domain/services/impact_analyzer.py
crosshair check src/agentic_workflow/domain/services/security_auditor.py
crosshair check src/agentic_workflow/domain/services/completion_checker.py
crosshair check src/agentic_workflow/domain/services/scope_analyzer.py
crosshair check src/agentic_workflow/domain/services/workflow_resumer.py
crosshair check src/agentic_workflow/domain/services/crosslink_tracer.py
crosshair check src/agentic_workflow/domain/algorithms/convergence.py
crosshair check src/agentic_workflow/domain/algorithms/micro_validation.py
crosshair check src/agentic_workflow/domain/algorithms/blast_radius.py
crosshair check src/agentic_workflow/domain/algorithms/rice_scoring.py
crosshair check src/agentic_workflow/domain/algorithms/trace_traversal.py
crosshair check src/agentic_workflow/domain/models/repo_map.py
crosshair check src/agentic_workflow/domain/models/model_config.py
crosshair check src/agentic_workflow/domain/services/hook_runner.py
crosshair check src/agentic_workflow/domain/services/llm_strategy_selector.py
crosshair check src/agentic_workflow/domain/algorithms/repo_map_builder.py
crosshair check src/agentic_workflow/domain/algorithms/context_budget.py
crosshair check src/agentic_workflow/domain/algorithms/model_selector.py
```

---

## 4. Coverage Matrix: INV → CLS → Method → Contract

| INV | CLS | Method | Contract Type | crosshair |
|-----|-----|--------|---------------|-----------|
| INV-001 | CLS-001 | advance() | ensure | ✅ |
| INV-002-v2 | CLS-001 | advance() | require | ✅ |
| INV-003 | CLS-002 | transition() | ensure | ✅ |
| INV-004 | CLS-002 | (class) | invariant | ✅ |
| INV-005-v2 | CLS-003 | auto_gate() | require | ✅ |
| INV-006 | CLS-004 | register() | require | ✅ |
| INV-007 | CLS-004 | add_upstream/downstream() | require | ✅ |
| INV-008 | CLS-005 | __init__() | require | ✅ |
| INV-009 | CLS-005 | __init__() | require | ✅ |
| INV-010 | CLS-006 | validate() | ensure | ✅ |
| INV-011 | CLS-006 | (class) | invariant | ✅ |
| INV-012 | ALG-003 | classify_severity() | ensure | ✅ |
| INV-013 | CLS-007 | analyze() | ensure | ✅ |
| INV-014 | CLS-008 | evaluate() | ensure | ✅ |
| INV-015 | ALG-004 | rice_score() | require+ensure | ✅ |
| INV-016 | CLS-011 | check_readiness() | ensure | ✅ |
| INV-017 | CLS-012 | analyze_charter() | ensure | ✅ |
| INV-018 | CLS-013 | apply_recovery() | ensure | ✅ |
| INV-019 | CLS-014 | trace_all_directions() | ensure | ✅ |
| INV-020 | CLS-016 | execute() | ensure | ✅ |
| INV-021 | ALG-007 | allocate_budget() | require+ensure | ✅ |
| INV-022 | CLS-017 | select() | ensure | ✅ |
| INV-023 | MCPGateway | auto_commit() | require+ensure | ✅ |
| INV-024 | ALG-006 | repo_map_build() | require+ensure | ✅ |

**Coverage**: 24/24 INV mapped to icontract decorators. 100%.
